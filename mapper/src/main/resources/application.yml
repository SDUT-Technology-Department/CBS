spring:
  datasource:
    druid:
      driver-class-name: com.mysql.jdbc.Driver
      url: jdbc:mysql://XXXXXXX?allowMultiQueries=true&useUnicode=true&characterEncoding=UTF-8
      username:
      password:

mybatis:
  mapper-locations: classpath:mappers/*Mapper.xml
  type-aliases-package: com.ky.ClassBrrowSystem.enity